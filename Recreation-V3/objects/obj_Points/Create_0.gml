/// @description Init Points

event_inherited();
image_speed = 0; // We don't want an animation, so this is 0.
gfunc_gameobj_priority_set(1);
pts = 0;

debug_name = "Points";	// Name displayed when being observed